<h1><%= @user.username %></h1>
<div class="card-deck-wrapper">
  <div class="card-deck">
    <%= render @user.recipes %>
  </div>
</div>

<p>
  <b>Image:</b>
  <%= link_to image_tag(@user.profile_image.thumb.url), @user.profile_image.url %>
</p>
